extends layout

prepend title
  - let admin = (username === players[0].username);
  - let section = t('stages.join');

block content
  div
    form(action="/next/"+gameid, method="POST")
      table(class="table table-striped table-bordered")
        - let index = 0
        thead
          tr
            th(colspan=(admin ? 3 : 2)) #{t('header_players_list')}
          tr
            th(style="width: 4%") #{t('header_number')}
            if admin
              th(style="width: 6%") #{t('header_include_player')}
            th #{t('header_name')}
        each player in players
          tr
            td(style="text-align:center") #{++index}
            if admin
              if index !== 1
                td(style="text-align:center")
                  input(
                    class="checkbox",
                    type="checkbox",
                    name="players[]",
                    value=player.username,
                    checked
                  )
              else
                td
            td(style="text-align:center")
              | #{player.name} #{index===1 ? t('admin_tag') : ''}
      if (new Date()) < enddate
        if !admin
          if players.map(p => p.username).includes(username)
            button(
              class="btn btn-primary",
              formaction="/leave/"+gameid,
              type='submit',
              formmethod="POST",
            ) #{t('leave_button')}
          else
            - let can_join = players.length<maxplayers || maxplayers===0;
            button(
              class="btn btn-primary",
              formaction="/join/"+gameid,
              type='submit',
              formmethod="POST",
              disabled=!can_join,
              title=(can_join ? false : t('game_full')),
            ) #{t('join_button')}
        div
          | #{t('end_date_join_alt')}
          |
          b #{enddate.toISOString().replace(/[A-Z].*/, '')}
      else if admin
        div
          | #{t('game_full_long')}
          button(
            class="btn btn-primary",
            formaction="/join/"+gameid,
            type='submit',
            formmethod="POST",
            title=t('deadline_reached'),
          ) #{t('deadline_button')}
      else
        div
          | #{t('end_date_join_alt')} (
          b #{enddate.toISOString().replace(/[A-Z].*/, '')}
          | )
      div
        | #{t('max_players')}
        |
        b #{maxplayers === 0 ? "None" : maxplayers}
      if admin
        - let can_start = players.length >= 3;
        button(
          class=(can_start ? "btn btn-primary" : "btn btn-disabled"),
          type='submit',
          disabled=!can_start,
          title=(can_start ? false : t('lacking_players')),
        ) #{t('start_button')}
